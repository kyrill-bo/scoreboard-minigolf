/**
 * @type {import('next').NextConfig}
 */

const nextConfig = {
    output: 'export',
    basePath: "/scoreboard-minigolf",
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
